

// XXX
// Project
//    data (possibly empty, the root is the union of all types) ->
//    through the schema, generate a tree annotated with state (valid/invalid+complete/incomplete) & children ->
//    through the type+mode2element map ->
//    rendering
